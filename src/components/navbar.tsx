/* eslint-disable @next/next/no-img-element */
import React, { useEffect, useRef } from "react";

interface Circle {
  x: number;
  y: number;
  size: number;
}

export const Navbar = () => {
  return (
    <nav className="bg-gray-900 fixed w-full h-[80px] z-20 top-0 left-0">

    </nav>
  );
};
